_PRELUDE(`mcaware')
_H1(`mcaware')

This is a non-exhaustive list of software I've written. The actual list is much longer, but there is a lot of lost source code. Everything here is largely unfinished, but also are largely "usable."

_LLIST(
_LLI(`libmca - a collection of code that you would have written anyway', _PAGE(`projects/libmca')),
_LLI(`newsd - RSS/Atom feed reader for the web', _PAGE(`projects/newsd')),
_LLI(`pikonet - centrally-managed WireGuard meshnets', _PAGE(`projects/pikonet')))
